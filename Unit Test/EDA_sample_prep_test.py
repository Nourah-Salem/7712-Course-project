# the unit test will be split into funtions that test every funtion within the EDA_sample_prep script
# the tests include the correct inupts to every function, check all possible conditions on the functions bevahiour and check the correctness of the retrived output

class EDA_sample_prep_test
  def FASTA2DF_test (self):
    # check the input (add other failing conditions
    # check the filing condiitons for the folloing functions:
    #.startswith
    #.append
    #.replace
    #.concat
    # check the resultant output for a correct output once and a failing condition
    
    
